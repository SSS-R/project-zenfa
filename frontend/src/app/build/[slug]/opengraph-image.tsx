import { ImageResponse } from 'next/og';

// Route segment config
export const runtime = 'edge';

// Image metadata
export const alt = 'PC Lagbe? Custom Build';
export const size = {
    width: 1200,
    height: 630,
};

export const contentType = 'image/png';

export default async function Image({ params }: { params: { slug: string } }) {
    // In a real app, fetch build details from an API using the slug:
    // const buildDetails = await fetch(`http://localhost:8001/builds/${params.slug}`).then((res) => res.json());

    // For now, generating a compelling placeholder using standard pricing logic
    const budget = "85,000";
    const useCase = "Gaming";
    const cpu = "AMD Ryzen 5 5600X";
    const gpu = "NVIDIA RTX 3060";

    return new ImageResponse(
        (
            <div
                style={{
                    fontSize: 48,
                    background: 'linear-gradient(to right bottom, #000000, #111111, #0a1f1c)',
                    color: 'white',
                    width: '100%',
                    height: '100%',
                    display: 'flex',
                    flexDirection: 'column',
                    alignItems: 'center',
                    justifyContent: 'center',
                    padding: '60px',
                    fontFamily: 'sans-serif',
                }}
            >
                <div
                    style={{
                        display: 'flex',
                        flexDirection: 'column',
                        alignItems: 'center',
                        justifyContent: 'center',
                        width: '100%',
                        height: '100%',
                        border: '2px solid rgba(79, 158, 151, 0.3)',
                        borderRadius: '40px',
                        padding: '40px',
                        backgroundColor: 'rgba(0, 0, 0, 0.4)',
                        boxShadow: '0 0 100px rgba(79, 158, 151, 0.1)',
                    }}
                >
                    {/* Header */}
                    <div style={{ display: 'flex', width: '100%', justifyContent: 'space-between', alignItems: 'center', marginBottom: '40px' }}>
                        <div style={{ fontSize: 64, fontWeight: 900, color: '#4f9e97', letterSpacing: '-0.02em' }}>
                            PC Lagbe?
                        </div>
                        <div style={{ fontSize: 32, color: '#888888', textTransform: 'uppercase', letterSpacing: '0.1em' }}>
                            {useCase} Build
                        </div>
                    </div>

                    {/* Main Content */}
                    <div style={{ display: 'flex', width: '100%', flex: 1, alignItems: 'center', justifyContent: 'center' }}>
                        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', textAlign: 'center' }}>
                            <div style={{ fontSize: 36, color: '#aaaaaa', marginBottom: '10px', textTransform: 'uppercase', letterSpacing: '0.05em' }}>Total Estimate</div>
                            <div style={{ fontSize: 110, fontWeight: 900, color: '#ffffff', marginBottom: '60px', textShadow: '0 10px 30px rgba(0,0,0,0.5)' }}>
                                ৳ {budget}
                            </div>

                            <div style={{ display: 'flex', gap: '30px' }}>
                                <div style={{ display: 'flex', padding: '20px 40px', background: 'rgba(255,255,255,0.05)', borderRadius: '20px', border: '1px solid rgba(255,255,255,0.1)' }}>
                                    <span style={{ color: '#4f9e97', fontWeight: 'bold', marginRight: '15px' }}>CPU:</span> {cpu}
                                </div>
                                <div style={{ display: 'flex', padding: '20px 40px', background: 'rgba(255,255,255,0.05)', borderRadius: '20px', border: '1px solid rgba(255,255,255,0.1)' }}>
                                    <span style={{ color: '#4f9e97', fontWeight: 'bold', marginRight: '15px' }}>GPU:</span> {gpu}
                                </div>
                            </div>
                        </div>
                    </div>

                    {/* Footer */}
                    <div style={{ display: 'flex', width: '100%', justifyContent: 'center', marginTop: '40px' }}>
                        <div style={{ fontSize: 24, color: '#666666' }}>
                            Generated by AI • Build yours at <span style={{ color: '#4f9e97' }}>pclagbe.com</span>
                        </div>
                    </div>
                </div>
            </div>
        ),
        {
            ...size,
        }
    );
}
